(function () {
  'use strict';

  var SharePoint = angular.module('ngSharePoint');

  SharePoint.factory('ngFolder', ['$resource', '$q', function ($resource, $q) {
    var ngFolder = {
    };

    return ngFolder;
  }]);

})();
