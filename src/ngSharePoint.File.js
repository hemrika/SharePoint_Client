(function () {
  'use strict';

  var SharePoint = angular.module('ngSharePoint');

  SharePoint.factory('ngFile', ['$resource', '$q', function ($resource, $q) {
    var ngFile = {
    };

    return ngFile;
  }]);

})();
